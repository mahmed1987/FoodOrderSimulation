package com.multibank.foodordersimulation.ui.theme

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.ui.unit.dp

val largePadding = PaddingValues(largeUnit)
val mediumPadding = PaddingValues(mediumUnit)
val smallPadding = PaddingValues(smallUnit)
val noPadding = PaddingValues(0.dp)
val smallStartBottomPadding = PaddingValues(start = smallUnit, bottom = smallUnit)
val mediumStartBottomPadding = PaddingValues(start = mediumUnit, bottom = mediumUnit)
val mediumStartVeryLargeBottomPadding = PaddingValues(start = 20.dp, bottom = veryLargeUnit)
val largeVerticalPadding = PaddingValues(top = largeUnit, bottom = largeUnit)